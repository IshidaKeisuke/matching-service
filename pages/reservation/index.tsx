import type { NextPage } from "next";
import { Calendar } from "../../components/calendar/Calendar";

const Home: NextPage = () => {
  return (
    <div>
      <Calendar />
    </div>
  );
};

export default Home;