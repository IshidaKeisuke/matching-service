import { Calendar } from "@/components/calendar/Calendar";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div>
      <Calendar />
    </div>
  );
};

export default Home;